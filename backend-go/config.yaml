geoserver:
  url: "http://localhost:8081/geoserver"
  workspace: "osm"
  username: "admin"
  password: "geoserver"

calliope:
  url: "http://localhost:5000"
  timeout: 600  # 10 minutes

server:
  port: 8082  # Backend API port (GeoServer uses 8080)
