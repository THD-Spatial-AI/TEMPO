# OSeMOSYS toolchain – otoole (CSV ↔ datafile conversion, result processing)
# https://otoole.readthedocs.io
# Requires Python 3.10+ (venv: osemosys-venv, service: osemosys_service.py, port 5004)
#
# The solver is GLPK's glpsol, bundled as a binary in solvers/ (not a pip package).
# The model file is vendored at python/osemosys/osemosys.txt — its schema must
# match the otoole version pinned here. Re-validate both together when bumping.

otoole==1.1.3

pandas>=2.0,<2.3
