# Calliope runner dependencies
# Install into a plain venv (no conda required):
#   python -m venv .venv-calliope
#   .venv-calliope\Scripts\activate   (Windows)
#   pip install -r python/requirements.txt
#
# Pinned packages intentionally minimal: calliope 0.6.8 declares its own
# strict constraints on numpy, pandas, xarray, plotly, scikit-learn, etc.
# Adding our own pins on top causes ResolutionImpossible errors.

calliope==0.6.8

# Extras not declared by calliope but needed by calliope_runner.py / service
pyomo>=6.0,<7.0    # LP/MIP modelling layer (calliope uses it but doesn't always pin)
highspy>=1.5       # HiGHS solver — open-source, no separate install needed

# Optional – nicer tracebacks in dev
ipython
ipdb
